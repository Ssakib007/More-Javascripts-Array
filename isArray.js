const friends = ["dan", "ann", "kane", "mann", "kan", "han"];
const num = [];
const food = "ros o gol";
const age = 15;

console.log(Array.isArray(friends));
// output is true
console.log(Array.isArray(num));
// output is true
console.log(Array.isArray(food));
// output is false
console.log(Array.isArray(age));
// output is false
